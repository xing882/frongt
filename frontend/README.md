# Web 前端（Vue 3 + Vite）

正式业务界面：Element Plus、ECharts、Axios，开发时通过 Vite 代理访问后端 `/api`（默认 `http://127.0.0.1:8765`）。

```bash
pnpm install
pnpm dev
```

浏览器访问终端提示的本地地址（一般为 `http://127.0.0.1:5173`）。接口说明见仓库根目录 `docs/API_FRONTEND.md`。
